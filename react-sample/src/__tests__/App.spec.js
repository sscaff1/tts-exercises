import App from '../App';

describe('Counter App', () => {
  it('should render', () => {});
  it('should increase the count by 1 when increase is pressed', () => {});
  it('should decrease the count by 1 when the decrease is pressed', () => {});
  it('should reset back to 0 when reset is pressed', () => {});
});
